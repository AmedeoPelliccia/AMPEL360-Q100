# =============================================================================
# ASIGT Configuration — {{ program_name }}
# =============================================================================
# Aircraft System Information Generative Transformer configuration
# Generated by AEROSPACEMODEL {{ aerospacemodel_version }}
# =============================================================================

asigt:
  program_id: "{{ program_id }}"
  model_code: "{{ model_code }}"
  
  brex:
    project_rules: "brex/{{ model_code }}_brex.yaml"
    master_authority: "../ASIT/GOVERNANCE/master_brex_authority.yaml"
    
  runs:
    output_path: "runs"
    log_format: "jsonl"
    
  settings:
    audit_logging: true
    hash_chain: true
    hitl_enabled: true
