# =============================================================================
# ASIT Configuration — {{ program_name }}
# =============================================================================
# Aircraft Standard Information Treatment configuration
# Generated by AEROSPACEMODEL {{ aerospacemodel_version }}
# =============================================================================

asit:
  program_id: "{{ program_id }}"
  model_code: "{{ model_code }}"
  
  governance:
    baselines_path: "GOVERNANCE"
    change_control_path: "GOVERNANCE/CHANGE_CONTROL"
    approvals_path: "GOVERNANCE/APPROVALS"
    releases_path: "GOVERNANCE/RELEASES"
    
  structure:
    ata_mapping: "STRUCTURE/ATA_MAPPING.yaml"
    lifecycle_phases: "STRUCTURE/LIFECYCLE_PHASES.yaml"
    naming_conventions: "STRUCTURE/NAMING_CONVENTIONS.md"
    
  contracts:
    schema: "CONTRACTS/CONTRACT_SCHEMA.yaml"
    templates_path: "CONTRACTS/templates"
    active_path: "CONTRACTS/active"
    
  index:
    ssot_index: "INDEX/SSOT_INDEX.yaml"
    trace_master: "INDEX/TRACE_MASTER.csv"
    id_registry: "INDEX/ID_REGISTRY.csv"
    artifact_catalog: "INDEX/ARTIFACT_CATALOG.yaml"
    
  standards:
    s1000d_issue: "{{ s1000d_issue }}"
    cage_code: "{{ cage_code }}"
    country_code: "{{ country_code }}"
    language_code: "{{ language_code }}"
